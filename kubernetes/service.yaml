apiVersion: v1
kind: Service

metadata:
  name: portfolio-service
  namespace: portfolio

spec:
  selector:
    app: portfolio

  type: LoadBalancer

  ports:
    - protocol: TCP
      port: 80
      targetPort: 80